services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_PASSWORD: koine
      POSTGRES_USER: koine
      POSTGRES_DB: koine
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
